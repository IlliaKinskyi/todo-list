import React from 'react';

type Props = {};

const Header = (props: Props) => {
  return (
    <div className="bg-primary-500 text-white h-7 w-full">
      <h1 className="pl-1">Header</h1>
    </div>
  );
};

export default Header;
